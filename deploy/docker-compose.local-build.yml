# Override for running the current local source code while preserving the
# existing local deployment data directories defined in docker-compose.local.yml.

services:
  subforai:
    image: subforai-local:latest
    build:
      context: ..
      dockerfile: Dockerfile
